// Alcance: /* if (true) { /     / var varVar = 'varVar'; / // disponible en toda la función     / let letVar = 'letVar'; / // disponible solo dentro del bloque if  /  } /      // Acercamiento:   / console.log(letVar); / // error de referencia, letVar no está disponible fuera del bloque  /  console.log(varVar) /; // muestra 'varVar'    // Contexto de ejecución:   / function fn() {     var varVar = 'varVar';   } /   / let letVar = 'letVar'; /   / console.log(varVar); / // muestra 'varVar', acoplado al contexto de ejecución global   / console.log(letVar); */ // muestra 'letVar', acoplado al contexto de ejecución local
